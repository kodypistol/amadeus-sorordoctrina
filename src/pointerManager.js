
// experienceManager.currentObject.rotation.y = 

// window.addEventListener('mousemove', (e) => {
//     e.clientX
// });

const start = 0 //

window.addEventListener('touchstart', (e) => {
    // console.log(e);
    // start = ?
});

window.addEventListener('touchmove',(e) => {
    // console.log(e);
    // experienceManager.currentObject.rotation.y =  distance * coef
});